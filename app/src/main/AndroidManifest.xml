<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kehel.controle_v1">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Controle_v1"
        android:usesCleartextTraffic="true">
        <activity android:name=".Note_c_u"></activity>
        <activity android:name=".Seance_per_user" />
        <activity android:name=".Seance_per_date" />
        <activity android:name=".Seance_per_week" />
        <activity android:name=".Seance_per_day" />
        <activity android:name=".Task_per_salarie" />
        <activity android:name=".Task_per_date" />
        <activity android:name=".Task_per_week" />
        <activity android:name=".Task_per_day" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".loginActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>